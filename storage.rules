rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Permitir lectura pública de todos los archivos del chat
    match /chat/{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
